import { CommandesDirective } from './commandes.directive';

describe('CommandesDirective', () => {
  it('should create an instance', () => {
    const directive = new CommandesDirective();
    expect(directive).toBeTruthy();
  });
});
